            {% if hero_image %}
            <div class="post-hero-wrapper">
                <div class="post-hero" aria-hidden="true">
                    <img src="{{ hero_image }}" alt="" />
                </div>
            {% endif %}
            <div class="post-header{% if hero_image %} post-header--hero{% endif %}">
                <h1>{{ title | markdownInline | safe }}</h1>
                {% if subtitle %}<p class="subtitle">{{ subtitle | markdownInline | safe }}</p>{% endif %}
                {% if layout == 'recipes' %}
                <p class="date">{{ page.inputPath | lastModifiedDate | postDate }}</p>
                {% elif date %}
                <p class="date">{{ date | postDate }}</p>
                {% endif %}
            </div>
            {% if hero_image %}
            </div>
            {% endif %}
